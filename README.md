# SSIS-to-dbt Migration Factory

> Automated conversion of SSIS (SQL Server Integration Services) packages to dbt (data build tool) models with validation and real-time monitoring.

[![Python 3.9+](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)
[![dbt](https://img.shields.io/badge/dbt-1.5+-orange.svg)](https://www.getdbt.com/)
[![React](https://img.shields.io/badge/react-19.x-61dafb.svg)](https://react.dev/)
[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

---

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        SSIS-to-dbt Migration Factory                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│   │   Phase 1   │───▶│   Phase 2   │───▶│   Phase 3   │───▶│   Phase 4   │     │
│   │   Parser    │    │  Scaffold   │    │  Validate   │    │  Dashboard  │     │
│   └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘     │
│         │                  │                  │                  │              │
│         ▼                  ▼                  ▼                  ▼              │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│   │ .dtsx files │    │ dbt models  │    │ Validation  │    │   React UI  │     │
│   │   → JSON    │    │ stg_, fct_  │    │   Reports   │    │  Monitoring │     │
│   └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Architecture

### High-Level Data Flow

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│    SSIS Packages              Migration Engine              dbt Project      │
│    ─────────────              ────────────────              ───────────      │
│                                                                              │
│    ┌────────────┐                                                            │
│    │ Customer   │─┐                                       ┌────────────────┐ │
│    │ DataLoad   │ │     ┌──────────────────────┐         │ models/        │ │
│    │ .dtsx      │ │     │                      │         │                │ │
│    └────────────┘ │     │   ┌──────────────┐   │         │ ├─ sources/    │ │
│                   ├────▶│   │ SSIS Parser  │   │────────▶│ │  └─ src_*.yml│ │
│    ┌────────────┐ │     │   │   (lxml)     │   │         │ │              │ │
│    │ SalesFact  │ │     │   └──────┬───────┘   │         │ ├─ staging/    │ │
│    │ ETL.dtsx   │─┤     │          │           │         │ │  └─ stg_*.sql│ │
│    └────────────┘ │     │   ┌──────▼───────┐   │         │ │              │ │
│                   │     │   │   Pydantic   │   │         │ └─ core/       │ │
│    ┌────────────┐ │     │   │   Models     │   │         │    ├─ fct_*.sql│ │
│    │ Inventory  │ │     │   └──────┬───────┘   │         │    ├─ dim_*.sql│ │
│    │ Sync.dtsx  │─┘     │          │           │         │    └─ agg_*.sql│ │
│    └────────────┘       │   ┌──────▼───────┐   │         └────────────────┘ │
│                         │   │ dbt Scaffold │   │                             │
│                         │   │  Generator   │   │                             │
│                         │   └──────────────┘   │                             │
│                         │                      │                             │
│                         └──────────────────────┘                             │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

### Component Mapping

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     SSIS → dbt Component Mapping                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   SSIS Component              Transforms To           dbt Equivalent        │
│   ──────────────              ─────────────           ──────────────        │
│                                                                             │
│   ┌──────────────┐                                   ┌──────────────┐       │
│   │ DataFlow     │  ─────────────────────────────▶   │ stg_*.sql    │       │
│   │ Task         │       staging model               │ (view)       │       │
│   └──────────────┘                                   └──────────────┘       │
│                                                                             │
│   ┌──────────────┐                                   ┌──────────────┐       │
│   │ Execute SQL  │  ─────────────────────────────▶   │ fct_*.sql    │       │
│   │ Task (MERGE) │       fact table                  │ (table)      │       │
│   └──────────────┘                                   └──────────────┘       │
│                                                                             │
│   ┌──────────────┐                                   ┌──────────────┐       │
│   │ Lookup       │  ─────────────────────────────▶   │ LEFT JOIN    │       │
│   │ Transform    │       join to ref()               │ ref('dim_')  │       │
│   └──────────────┘                                   └──────────────┘       │
│                                                                             │
│   ┌──────────────┐                                   ┌──────────────┐       │
│   │ Derived      │  ─────────────────────────────▶   │ CAST / SQL   │       │
│   │ Column       │       expression                  │ expression   │       │
│   └──────────────┘                                   └──────────────┘       │
│                                                                             │
│   ┌──────────────┐                                   ┌──────────────┐       │
│   │ Script Task  │  ─────────────────────────────▶   │ ⚠️ Manual    │       │
│   │ / FTP / etc  │       flagged for review          │ Review       │       │
│   └──────────────┘                                   └──────────────┘       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Monitoring Dashboard

### Main Dashboard View

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  ☰  SSIS-to-dbt Migration Dashboard                          🌙 Dark  ↻ Refresh │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐                     │
│  │ 📦 Packages │ 📊 Tasks    │ ✅ Converted │ ⚠️ Review   │                     │
│  │     3       │    12       │     9       │     3       │                     │
│  └─────────────┴─────────────┴─────────────┴─────────────┘                     │
│                                                                                 │
│  ┌─ Tabs ──────────────────────────────────────────────────────────────────┐   │
│  │  [Packages]    [Live Logs]    [Validation]    [SQL Diff]                │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
│  ┌─ Packages ───────────────────────────────────────────────────────────────┐  │
│  │                                                                          │  │
│  │  ▼ CustomerDataLoad.dtsx                              ✅ Migrated       │  │
│  │    ├─ Connection Managers: 2                                            │  │
│  │    ├─ Data Flow Tasks: 1                                                │  │
│  │    ├─ Execute SQL Tasks: 2                                              │  │
│  │    └─ Manual Review Items: 0                                            │  │
│  │                                                                          │  │
│  │  ▶ SalesFactETL.dtsx                                  ✅ Migrated       │  │
│  │                                                                          │  │
│  │  ▶ InventorySync.dtsx                                 ⚠️ Needs Review   │  │
│  │                                                                          │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### Validation View

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  Validation Results                                                             │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐                     │
│  │ 📊 Models   │ ✅ Passed   │ ❌ Failed   │ ⚠️ Warning  │                     │
│  │     4       │     4       │     0       │     0       │                     │
│  └─────────────┴─────────────┴─────────────┴─────────────┘                     │
│                                                                                 │
│  ┌───────────────────────────────────────────────────────────────────────────┐ │
│  │ Model              │ Row Count │ PK Check │ Checksum │ Status             │ │
│  ├───────────────────────────────────────────────────────────────────────────┤ │
│  │ dim_customer       │   ✅ OK   │   ✅ OK  │   ✅ OK  │ ✅ PASSED          │ │
│  │ fct_sales          │   ✅ OK   │   ✅ OK  │   ✅ OK  │ ✅ PASSED          │ │
│  │ fct_inventory      │   ✅ OK   │   ✅ OK  │   ✅ OK  │ ✅ PASSED          │ │
│  │ agg_daily_sales    │   ✅ OK   │   ✅ OK  │   ✅ OK  │ ✅ PASSED          │ │
│  └───────────────────────────────────────────────────────────────────────────┘ │
│                                                                                 │
│  ▼ dim_customer - Validation Details                                           │
│  ┌───────────────────────────────────────────────────────────────────────────┐ │
│  │  Row Count Comparison                                                     │ │
│  │  ├─ Legacy Table:  12,847 rows                                           │ │
│  │  ├─ dbt Model:     12,847 rows                                           │ │
│  │  └─ Variance:      0.00%                                                 │ │
│  │                                                                           │ │
│  │  Primary Key Validation                                                   │ │
│  │  ├─ Null Count:    0                                                     │ │
│  │  └─ Duplicates:    0                                                     │ │
│  │                                                                           │ │
│  │  Numeric Checksums (SUM/AVG comparison)                                   │ │
│  │  └─ All columns within 0.01% tolerance                                   │ │
│  └───────────────────────────────────────────────────────────────────────────┘ │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### SQL Diff View

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  SQL Comparison: SalesFactETL / Load Sales Facts                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─ Original SSIS SQL ──────────────┐ ┌─ Generated dbt Model ─────────────────┐│
│  │                                  │ │                                       ││
│  │ MERGE INTO dw.fact_sales t       │ │ {{ config(                            ││
│  │ USING (                          │ │     materialized='table',             ││
│  │   SELECT                         │ │     schema='core'                     ││
│  │     o.order_id,                  │ │ ) }}                                  ││
│  │     o.customer_id,               │ │                                       ││
│  │     o.product_id,                │ │ WITH source_data AS (                 ││
│  │     o.order_date,                │ │   SELECT                              ││
│  │     o.quantity,                  │ │     order_id,                         ││
│  │     o.unit_price,                │ │     customer_id,                      ││
│  │     o.quantity * o.unit_price    │ │     product_id,                       ││
│  │       AS total_amount            │ │     order_date,                       ││
│  │   FROM staging.orders o          │ │     quantity,                         ││
│  │ ) s                              │ │     unit_price,                       ││
│  │ ON t.order_id = s.order_id       │ │     quantity * unit_price             ││
│  │ WHEN MATCHED THEN UPDATE...      │ │       AS total_amount                 ││
│  │ WHEN NOT MATCHED THEN INSERT...  │ │   FROM {{ ref('stg_sales') }}         ││
│  │                                  │ │ )                                     ││
│  │                                  │ │ SELECT * FROM source_data             ││
│  └──────────────────────────────────┘ └───────────────────────────────────────┘│
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Project Structure

```
ssis-to-dbt/
├── src/
│   ├── parser/
│   │   ├── ssis_parser.py      # Main parser (XXE-hardened)
│   │   ├── models.py           # Pydantic data models
│   │   ├── constants.py        # XML namespaces, mappings
│   │   └── type_mappings.py    # SSIS → SQL type conversions
│   └── validation/
│       ├── validator.py        # Post-migration validator
│       ├── models.py           # Validation result models
│       ├── remediation_agent.py # Self-healing agent
│       └── remediation_models.py # Remediation data models
│
├── dbt_project/
│   ├── dbt_project.yml         # dbt configuration
│   ├── packages.yml            # dbt dependencies
│   └── models/
│       ├── sources/            # Source definitions (src_*.yml)
│       ├── staging/            # Staging models (stg_*.sql)
│       └── core/               # Fact/Dim models (fct_*, dim_*, agg_*)
│
├── ui/                         # React monitoring dashboard
│   ├── src/
│   │   ├── components/         # Dashboard, PackageList, ValidationView
│   │   ├── types/              # TypeScript interfaces
│   │   └── data/               # Mock JSON data
│   └── package.json
│
├── samples/
│   └── ssis_packages/          # Sample .dtsx files
│
├── output/                     # Generated reports
│   ├── parsed_packages.json
│   ├── migration_mapping.json
│   ├── validation_log.json
│   └── *.md reports
│
├── run_migration.py            # Main orchestration script
├── requirements.txt            # Python dependencies
└── CLAUDE.md                   # AI assistant instructions
```

---

## Installation

### Prerequisites

- Python 3.9+
- Node.js 18+ (for dashboard)
- dbt-core 1.5+
- SQL Server connection (for dbt execution)

### Setup

```bash
# Clone the repository
git clone https://github.com/your-org/ssis-to-dbt.git
cd ssis-to-dbt

# Install Python dependencies
pip install -r requirements.txt

# Install dbt packages
cd dbt_project
dbt deps
cd ..

# Install UI dependencies (optional)
cd ui
npm install
cd ..
```

---

## Usage

### 1. Parse SSIS Packages

```bash
# Parse all .dtsx files in a directory
python3 -m src.parser.ssis_parser ./samples/ssis_packages -o ./output -v

# Output files generated:
#   - output/parsed_packages.json
#   - output/schema_metadata.json
#   - output/parsing_report.md
```

### 2. Run Migration Pipeline

```bash
# Full migration with validation
python3 run_migration.py

# Skip dbt execution (parse only)
python3 run_migration.py --skip-dbt

# Skip validation
python3 run_migration.py --skip-validation

# Verbose output
python3 run_migration.py -v

# Enable auto-remediation (retries up to 3 times)
python3 run_migration.py --auto-fix

# Custom max attempts and tolerance
python3 run_migration.py --auto-fix --max-attempts 5 --tolerance 0.5
```

### 3. Run dbt Models

```bash
cd dbt_project

# Install dependencies
dbt deps

# Run all models
dbt run

# Run tests
dbt test

# Run and test together
dbt build
```

### 4. Launch Monitoring Dashboard

```bash
cd ui
npm run dev
# Open http://localhost:5173
```

---

## Type Mappings

| SSIS Type       | SQL Server Type | Notes                    |
|-----------------|-----------------|--------------------------|
| DT_WSTR         | NVARCHAR        | Unicode string           |
| DT_STR          | VARCHAR         | ANSI string              |
| DT_I4           | INT             | 32-bit integer           |
| DT_I8           | BIGINT          | 64-bit integer           |
| DT_NUMERIC      | NUMERIC(p,s)    | Precision/scale preserved|
| DT_DBTIMESTAMP  | DATETIME        | Date and time            |
| DT_BOOL         | BIT             | Boolean                  |
| DT_GUID         | UNIQUEIDENTIFIER| GUID/UUID                |

---

## Naming Conventions

| Layer      | Pattern                    | Example                    |
|------------|----------------------------|----------------------------|
| Sources    | `src_{system}_{table}`     | `src_crm_customers`        |
| Staging    | `stg_{domain}__{entity}`   | `stg_sales__transactions`  |
| Facts      | `fct_{business_process}`   | `fct_sales`                |
| Dimensions | `dim_{entity}`             | `dim_customer`             |
| Aggregates | `agg_{grain}_{subject}`    | `agg_daily_sales`          |

---

## Validation Checks

The migration validator performs three types of checks:

| Check Type       | Description                                    | Pass Criteria        |
|------------------|------------------------------------------------|----------------------|
| Row Count        | Compare legacy table vs dbt model row counts   | Variance < 0.1%      |
| Primary Key      | Check for NULL and duplicate primary keys      | 0 nulls, 0 duplicates|
| Numeric Checksum | Compare SUM/AVG of numeric columns             | Variance < 0.01%     |

---

## Auto-Remediation Agent

The system includes a self-healing agent that automatically diagnoses and fixes validation failures.

### How It Works

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          Auto-Remediation Flow                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐  │
│   │  Validate   │────▶│  Diagnose   │────▶│   Plan &    │────▶│  Re-run     │  │
│   │  dbt vs DWH │     │  Root Cause │     │   Apply Fix │     │  Validation │  │
│   └─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘  │
│         │                                                            │          │
│         │                        ┌───────────────────────────────────┘          │
│         │                        │                                              │
│         │                        ▼                                              │
│         │              ┌──────────────────┐                                     │
│         │              │  Still failing?  │                                     │
│         │              └────────┬─────────┘                                     │
│         │                       │                                               │
│         │         Yes, retry    │    No, all passed                            │
│         │         (max 3x)      │                                               │
│         │        ┌──────────────┴──────────────┐                               │
│         │        ▼                             ▼                                │
│         │  ┌───────────┐               ┌─────────────┐                         │
│         │  │  Retry or │               │   Success!  │                         │
│         │  │  Give Up  │               │             │                         │
│         │  └───────────┘               └─────────────┘                         │
│         │        │                                                              │
│         │        ▼ (after 3 failures)                                          │
│         │  ┌─────────────────────────────┐                                     │
│         │  │  Stop & Report Issues       │                                     │
│         │  │  - List failing models      │                                     │
│         │  │  - Explain root causes      │                                     │
│         │  │  - Recommend manual fixes   │                                     │
│         │  └─────────────────────────────┘                                     │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### Discrepancy Types Detected

| Type                  | Detection                          | Auto-Fix Strategy              |
|-----------------------|------------------------------------|--------------------------------|
| Row Count (Over)      | dbt > legacy rows                  | Add DISTINCT, review filters   |
| Row Count (Under)     | dbt < legacy rows                  | Fix JOINs, relax WHERE clause  |
| PK Nulls              | NULL values in primary key         | Add COALESCE or filter NULLs   |
| PK Duplicates         | Duplicate primary keys             | Add DISTINCT, fix JOIN logic   |
| Checksum Variance     | SUM/AVG differs from legacy        | Fix type casting, NULL handling|

### Usage

```bash
# Enable auto-remediation with default settings (3 attempts, 1% tolerance)
python3 run_migration.py --auto-fix

# Customize retry behavior
python3 run_migration.py --auto-fix --max-attempts 5 --tolerance 0.5

# Verbose mode for detailed diagnosis output
python3 run_migration.py --auto-fix -v
```

### Output

When remediation completes (success or failure), the system generates:

- `output/remediation_report.md` - Detailed remediation history
- Console output with:
  - Discrepancy analysis
  - Root cause diagnosis
  - Applied fixes
  - Final status and recommendations

### Example Output (Failure After 3 Attempts)

```
╭─────────────────────────────────────────────────────────────────╮
│ Remediation Incomplete                                          │
│                                                                 │
│ After 3 attempts, 2 model(s) still failing.                    │
│                                                                 │
│ Remaining Failures:                                             │
│   • fct_sales                                                   │
│   • agg_daily_sales                                             │
╰─────────────────────────────────────────────────────────────────╯

╭─────────────────────────────────────────────────────────────────╮
│ Manual Review Required                                          │
│                                                                 │
│ The following models need manual intervention:                  │
│                                                                 │
│ • fct_sales: Row count 5.2% higher than legacy - likely        │
│   duplicate joins or missing business filter                    │
│                                                                 │
│ • agg_daily_sales: Checksum variance 3.1% on total_amount -    │
│   check decimal precision in type casting                       │
╰─────────────────────────────────────────────────────────────────╯

Recommended Next Steps:
1. Review the validation_report.md for detailed discrepancy information
2. Compare legacy SQL queries with generated dbt models
3. Check source data for any recent changes or anomalies
4. Manually adjust dbt models based on the diagnosis above
```

---

## Manual Review Items

Some SSIS components require manual review and cannot be auto-converted:

| Component Type    | Reason                                    | Recommendation              |
|-------------------|-------------------------------------------|-----------------------------|
| Script Task       | Contains custom C#/VB.NET code            | Convert to dbt macro/Python |
| Send Mail Task    | Email notification                        | Use external alerting       |
| FTP Task          | File transfer operations                  | Use dbt external tables     |
| Execute Process   | External process execution                | Orchestration layer         |
| File System Task  | File operations                           | Pre/post hooks              |

---

## Security

- **XXE Protection**: The SSIS parser uses hardened XML parsing with external entity resolution disabled
- **No Network Access**: XML parser configured with `no_network=True`
- **DTD Validation Disabled**: Prevents DTD-based attacks

---

## Contributing

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'Add amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

---

## License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

---

## Acknowledgments

- [dbt Labs](https://www.getdbt.com/) for the amazing data transformation framework
- [lxml](https://lxml.de/) for secure XML parsing
- [Pydantic](https://pydantic.dev/) for data validation
