# SQL Server Connection
# IMPORTANT: Change all default values before use!
MSSQL_SERVER=localhost
MSSQL_PORT=1433
MSSQL_DATABASE=SalesDB
MSSQL_USER=sa
# SECURITY: Generate a strong password with at least 16 characters,
# including uppercase, lowercase, numbers, and special characters
# Example: openssl rand -base64 24
MSSQL_PASSWORD=CHANGE_THIS_PASSWORD_BEFORE_USE

# OpenAI API Key (optional - for LLM-enhanced analysis)
# OPENAI_API_KEY=sk-your-api-key-here

# For production, use a non-sa user with minimal required permissions
